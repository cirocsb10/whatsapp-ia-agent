"use client";
import { ChannelsSettingsPanel } from "@/features/channels/components/ChannelsSettingsPanel";

/** Settings tab: números WhatsApp conectados (multicanal). */
export function TabNumeros() {
  return <ChannelsSettingsPanel />;
}
